# Agent 说明（Shiloku 个人站）

本仓库已配置 Matt Pocock 工程技能包。代理写代码前请先读本节。

## 快速帮助

在 Cursor 对话里输入 **`/help`**，会用大白话列出所有技能和本项目用法。不知道选哪个时用 **`/ask-matt`**。

## Agent skills

### Issue tracker

任务与需求文档存放在本仓库 `.scratch/` 目录下的 Markdown 文件（本地模式，无需 GitHub Issues）。详见 `docs/agents/issue-tracker.md`。

### Triage labels

整理任务时使用的五种状态标签见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
